import { SumColumnPipe } from './sum-column.pipe';

describe('SumColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new SumColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
